(window.FRAMEWORKS = window.FRAMEWORKS || []).push({
  id: "ml-evaluation",
  name: "Model Evaluation & Selection",
  language: "Classical ML",
  group: "Classical Machine Learning",
  navLabel: "Evaluation & Selection",
  tagline: "The skills that separate practitioners from tutorial-followers — metrics, cross-validation, tuning, leakage, and honest model comparison.",
  color: "#34D399",
  readMinutes: 50,
  sections: [
    {
      id: "why-hard",
      title: "Why evaluation is the hard part",
      level: "core",
      body: [
        { type: "p", text: "Anyone can call `model.fit(X, y)`. The skill that actually separates practitioners from tutorial-followers is knowing whether the resulting model is any good — and knowing it *before* it fails in production. Training a model is the easy 20%. Evaluating it honestly, choosing between candidates, and not fooling yourself is the hard 80% that never shows up in a Kaggle leaderboard screenshot." },
        { type: "p", text: "The central problem is that the number you can measure is not the number you care about. You care about **future performance on data you have never seen**. What you can compute is performance on data you *have* seen. The entire discipline of evaluation is a set of tricks for making the second number an honest estimate of the first." },
        { type: "heading", text: "The generalization gap" },
        { type: "p", text: "Formally, we want to minimize the **expected risk** — the average loss over the true (unknown) data distribution $\\mathcal{D}$ — but we can only ever compute the **empirical risk** on a finite sample:" },
        { type: "math", tex: String.raw`R(f) = \mathbb{E}_{(x,y)\sim\mathcal{D}}\big[\ell(f(x), y)\big] \quad\text{vs.}\quad \hat R(f) = \frac{1}{m}\sum_{i=1}^m \ell(f(x_i), y_i)` },
        { type: "p", text: "The difference $R(f) - \\hat R(f)$ is the **generalization gap**. Training error is a wildly optimistic estimate of $R(f)$ because the model was tuned to make exactly that number small. Every technique in this chapter — held-out sets, cross-validation, leakage hygiene — exists to estimate $R(f)$ without cheating." },
        { type: "heading", text: "Goodhart's law: the metric you optimize stops measuring what you meant" },
        { type: "p", text: "**\"When a measure becomes a target, it ceases to be a good measure.\"** The moment you select models by a metric, the metric is no longer an unbiased picture of quality — you have optimized *toward it*, including its blind spots. Optimize accuracy on a 99%-negative dataset and you get a model that predicts \"negative\" always: 99% accurate, completely useless." },
        { type: "callout", variant: "warn", text: "**The test set is a consumable resource.** Every time you look at test performance and change something, you leak a little information from the test set into your decisions. Do it enough and your \"held-out\" test score is just training score wearing a disguise. Touch the test set once, at the very end. Everything before that is validation." },
        { type: "callout", variant: "note", text: "**How to read this chapter.** Each metric is shown three ways: the *intuition* (what question it answers), the *formula* (derived, not memorized), and the *code* (from scratch in NumPy, then the scikit-learn one-liner). The goal is that when a stakeholder asks \"why did you use F1 and not accuracy?\", you have a crisp, correct answer." },
      ]
    },

    {
      id: "classification-metrics",
      title: "Classification metrics: the confusion matrix and its children",
      level: "core",
      body: [
        { type: "p", text: "Almost every classification metric is a different way of squeezing four numbers into one. Those four numbers are the **confusion matrix**. Master it and precision, recall, F1, specificity, and accuracy all become trivial rearrangements rather than things to memorize." },
        { type: "heading", text: "The four outcomes" },
        { type: "p", text: "For a binary classifier, every prediction lands in one of four cells depending on the true label and the predicted label:" },
        { type: "table",
          headers: ["", "Predicted Positive", "Predicted Negative"],
          rows: [
            ["**Actual Positive**", "True Positive (TP)", "False Negative (FN) — a *miss*"],
            ["**Actual Negative**", "False Positive (FP) — a *false alarm*", "True Negative (TN)"],
          ]
        },
        { type: "p", text: "FN and FP are the two ways to be wrong, and they usually cost wildly different amounts. A false negative on a cancer screen (told healthy, actually sick) is catastrophic; a false positive (told sick, actually healthy) means an anxious follow-up test. A metric that treats them the same is lying to you." },
        { type: "heading", text: "Precision — of the alarms I raised, how many were real?" },
        { type: "math", tex: String.raw`\text{Precision} = \frac{TP}{TP + FP}` },
        { type: "p", text: "Precision punishes **false alarms**. It answers: when the model says positive, how often is it right? You optimize precision when acting on a positive prediction is expensive — flagging a transaction as fraud freezes a customer's card, so you had better be sure." },
        { type: "heading", text: "Recall (sensitivity, TPR) — of the real positives, how many did I catch?" },
        { type: "math", tex: String.raw`\text{Recall} = \frac{TP}{TP + FN}` },
        { type: "p", text: "Recall punishes **misses**. It answers: of all the actual positives out there, what fraction did the model find? You optimize recall when *missing* a positive is expensive — a missed tumor, a missed security breach. The cost of the follow-up is small compared to the cost of letting one through." },
        { type: "callout", variant: "tip", text: "**The precision–recall tension.** These two pull in opposite directions. Flag everything as positive → recall = 100%, precision in the gutter. Flag only the one case you are certain about → precision = 100%, recall near zero. Every classifier lives on this tradeoff curve; choosing *where* on it is a business decision, not a modeling one." },
        { type: "heading", text: "F1 — the harmonic mean that forces a compromise" },
        { type: "p", text: "When you need a single number that rewards being good at *both* precision and recall, use the **F1 score** — their harmonic mean:" },
        { type: "math", tex: String.raw`F_1 = 2 \cdot \frac{\text{Precision} \cdot \text{Recall}}{\text{Precision} + \text{Recall}} = \frac{2\,TP}{2\,TP + FP + FN}` },
        { type: "p", text: "Why the *harmonic* mean and not the ordinary average? The harmonic mean is dominated by the smaller value. If precision is 1.0 and recall is 0.0, the arithmetic mean is a misleadingly rosy 0.5, but $F_1 = 0$. F1 refuses to let you hide a terrible score behind a great one. The generalized $F_\\beta$ lets you weight recall $\\beta$ times as important as precision:" },
        { type: "math", tex: String.raw`F_\beta = (1 + \beta^2) \cdot \frac{\text{Precision} \cdot \text{Recall}}{\beta^2 \cdot \text{Precision} + \text{Recall}}` },
        { type: "p", text: "$F_2$ weights recall higher (use when misses hurt more); $F_{0.5}$ weights precision higher (use when false alarms hurt more)." },
        { type: "heading", text: "Specificity — recall for the negative class" },
        { type: "math", tex: String.raw`\text{Specificity} = \frac{TN}{TN + FP} = 1 - \text{FPR}` },
        { type: "p", text: "Specificity (true negative rate) is how well you identify the negatives. Sensitivity and specificity are the medical world's favorite pair: a *sensitive* test rarely misses disease; a *specific* test rarely false-alarms on the healthy." },
        { type: "heading", text: "Accuracy — and the trap that ruins careers" },
        { type: "math", tex: String.raw`\text{Accuracy} = \frac{TP + TN}{TP + TN + FP + FN}` },
        { type: "callout", variant: "gotcha", text: "**Accuracy is a trap on imbalanced data.** If 99.5% of transactions are legitimate, a model that predicts \"legitimate\" for *every* transaction scores 99.5% accuracy while catching exactly zero fraud. Accuracy only makes sense when classes are roughly balanced *and* the two error types cost the same. On imbalanced problems, report precision/recall/F1 or PR-AUC — never accuracy alone." },
        { type: "heading", text: "From scratch, then scikit-learn" },
        { type: "code", lang: "py", code: "import numpy as np\n\ndef confusion(y_true, y_pred):\n    tp = int(np.sum((y_pred == 1) & (y_true == 1)))\n    fp = int(np.sum((y_pred == 1) & (y_true == 0)))\n    fn = int(np.sum((y_pred == 0) & (y_true == 1)))\n    tn = int(np.sum((y_pred == 0) & (y_true == 0)))\n    return tp, fp, fn, tn\n\ndef metrics(y_true, y_pred):\n    tp, fp, fn, tn = confusion(y_true, y_pred)\n    precision = tp / (tp + fp) if tp + fp else 0.0\n    recall    = tp / (tp + fn) if tp + fn else 0.0   # = sensitivity = TPR\n    specificity = tn / (tn + fp) if tn + fp else 0.0\n    f1 = 2 * precision * recall / (precision + recall) if precision + recall else 0.0\n    accuracy = (tp + tn) / (tp + tn + fp + fn)\n    return dict(precision=precision, recall=recall,\n               specificity=specificity, f1=f1, accuracy=accuracy)\n\n# A 99.5% imbalanced problem where accuracy lies:\ny_true = np.array([0]*995 + [1]*5)\ny_pred = np.zeros(1000, dtype=int)          # 'always negative'\nprint(metrics(y_true, y_pred))\n# accuracy=0.995 but recall=0.0 -> caught none of the 5 positives." },
        { type: "code", lang: "py", code: "from sklearn.metrics import (confusion_matrix, precision_score,\n                             recall_score, f1_score, classification_report)\n\nprint(confusion_matrix(y_true, y_pred))     # [[995, 0], [5, 0]]\nprint(precision_score(y_true, y_pred, zero_division=0))  # 0.0\nprint(recall_score(y_true, y_pred))                      # 0.0\nprint(f1_score(y_true, y_pred))                          # 0.0\n\n# classification_report gives per-class precision/recall/F1 + support:\nprint(classification_report(y_true, y_pred, zero_division=0))" },
        { type: "callout", variant: "note", text: "**Multiclass averaging.** With more than two classes, `average='macro'` averages the per-class F1 equally (every class counts the same — good when small classes matter), while `average='weighted'` weights by class frequency (dominated by big classes), and `average='micro'` pools all TP/FP/FN globally (equals accuracy for single-label problems). Choosing the wrong average silently changes the story." },
      ]
    },

    {
      id: "roc-pr",
      title: "ROC & precision–recall curves: evaluating across all thresholds",
      level: "core",
      body: [
        { type: "p", text: "Every metric above assumed a fixed decision threshold — usually 0.5. But a classifier does not output a class; it outputs a **score** (a probability or margin), and *you* choose the threshold that turns it into a decision. Lower the threshold and recall rises while precision falls. ROC and PR curves evaluate the model across *every* threshold at once, decoupling the model's quality from any single cutoff." },
        { type: "heading", text: "The two rates that define the ROC curve" },
        { type: "p", text: "Sweep the threshold from high to low and plot two quantities against each other:" },
        { type: "math", tex: String.raw`\text{TPR} = \frac{TP}{TP + FN}\;=\;\text{recall}, \qquad \text{FPR} = \frac{FP}{FP + TN}\;=\;1 - \text{specificity}` },
        { type: "p", text: "The **ROC curve** (Receiver Operating Characteristic — the name is a WWII radar artifact) plots TPR on the y-axis against FPR on the x-axis. At threshold $=\\infty$ you predict nothing positive (bottom-left, $0,0$); at threshold $=-\\infty$ you predict everything positive (top-right, $1,1$). A good model bows toward the top-left corner: high true-positive rate at low false-positive cost." },
        { type: "heading", text: "ROC-AUC and its beautiful probabilistic meaning" },
        { type: "p", text: "The **area under the ROC curve** collapses the whole curve into one number in $[0.5, 1]$. A random classifier traces the diagonal ($\\text{AUC} = 0.5$); a perfect one reaches the corner ($\\text{AUC} = 1$). Its interpretation is exact and worth memorizing:" },
        { type: "math", tex: String.raw`\text{AUC} = P\big(\text{score}(x^+) > \text{score}(x^-)\big)` },
        { type: "p", text: "**ROC-AUC is the probability that a randomly chosen positive example is ranked above a randomly chosen negative one.** It measures *ranking quality*, is threshold-independent, and is invariant to the class balance of your evaluation set — which is both its strength and, on rare-positive problems, its weakness." },
        { type: "callout", variant: "gotcha", text: "**ROC-AUC is over-optimistic on severe imbalance.** Because FPR has the huge negative count $TN$ in its denominator, a flood of false positives barely moves the x-axis. A fraud model can post 0.95 ROC-AUC while its precision is dismal. When positives are rare and precision is what you care about, use the **precision–recall curve** instead." },
        { type: "heading", text: "Precision–recall curves for imbalanced problems" },
        { type: "p", text: "The PR curve plots precision (y) against recall (x) across thresholds. Its area, **PR-AUC** (a.k.a. average precision), focuses entirely on the positive class and ignores the true-negative mountain. On a 1%-positive problem, the PR curve's baseline is the positive rate (0.01), so beating it is meaningful in a way ROC's fixed 0.5 baseline is not." },
        { type: "table",
          headers: ["Use ROC-AUC when…", "Use PR-AUC when…"],
          rows: [
            ["Classes are roughly balanced", "Positives are rare (fraud, disease, churn)"],
            ["You care about both classes equally", "You care mainly about the positive class"],
            ["You want a class-balance-invariant score", "False positives among rare positives are costly"],
          ]
        },
        { type: "heading", text: "Choosing the operating threshold" },
        { type: "p", text: "ROC/PR-AUC pick the best *model*; you still must pick a *threshold* to deploy. Options: (1) maximize F1 across thresholds; (2) hit a required recall (e.g. \"catch 95% of fraud\") and read off the resulting precision; (3) maximize **Youden's J** $= \\text{TPR} - \\text{FPR}$ for a balanced ROC operating point; (4) minimize expected cost when you can put dollar values on FP and FN." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.metrics import roc_curve, roc_auc_score, precision_recall_curve, average_precision_score\n\n# scores = model.predict_proba(X_test)[:, 1]\ny = np.array([0,0,1,0,1,1,0,1,1,1])\nscores = np.array([.1,.4,.35,.8,.7,.9,.2,.6,.55,.95])\n\nfpr, tpr, thr = roc_curve(y, scores)\nprint('ROC-AUC :', roc_auc_score(y, scores))\nprint('PR-AUC  :', average_precision_score(y, scores))\n\n# Threshold that maximizes Youden's J = TPR - FPR:\nj = tpr - fpr\nbest = thr[np.argmax(j)]\nprint('best threshold (Youden J):', round(float(best), 3))\n\n# Or: smallest threshold meeting a recall requirement of >= 0.90\nprec, rec, pr_thr = precision_recall_curve(y, scores)\nok = rec[:-1] >= 0.90               # last point has no threshold\nif ok.any():\n    idx = np.where(ok)[0][-1]\n    print(f'to hit 90% recall use thr={pr_thr[idx]:.3f}, precision={prec[idx]:.3f}')" },
        { type: "callout", variant: "tip", text: "**AUC compares rankings, not calibration.** A model can have perfect AUC and terrible *probabilities* (all scores squished near 0.5, just in the right order). If you need the numbers to mean literal probabilities — for expected-cost decisions or downstream math — check calibration (a reliability diagram / Brier score) and fix it with `CalibratedClassifierCV`." },
      ]
    },

    {
      id: "regression-metrics",
      title: "Regression metrics: MSE, RMSE, MAE, R² and residuals",
      level: "core",
      body: [
        { type: "p", text: "When the target is continuous, you measure the *size* of the errors rather than counting hits and misses. The choices differ mainly in how they treat large errors and whether their units are interpretable." },
        { type: "heading", text: "MSE and RMSE — squared error, and the same in real units" },
        { type: "math", tex: String.raw`\text{MSE} = \frac{1}{m}\sum_{i=1}^m (y_i - \hat y_i)^2, \qquad \text{RMSE} = \sqrt{\text{MSE}}` },
        { type: "p", text: "Squaring means a single 10-unit error contributes as much as a hundred 1-unit errors — MSE is **dominated by outliers**. That is a feature when big misses are disproportionately bad, and a bug when a few noisy labels drag your metric around. RMSE just square-roots MSE to restore the target's units (dollars, not dollars-squared), so it is the reportable version. MSE is also what ordinary least squares literally minimizes, which is why it is the default training loss." },
        { type: "heading", text: "MAE — robust, linear, interpretable" },
        { type: "math", tex: String.raw`\text{MAE} = \frac{1}{m}\sum_{i=1}^m |y_i - \hat y_i|` },
        { type: "p", text: "Mean absolute error weights every error proportionally, so it is **robust to outliers**. \"MAE = 3.2\" reads directly as \"typically off by 3.2 units.\" The subtle statistical difference: minimizing MSE chases the conditional *mean*, while minimizing MAE chases the conditional *median*. On skewed targets that changes what \"best\" even means." },
        { type: "heading", text: "R² — fraction of variance explained" },
        { type: "p", text: "The **coefficient of determination** compares your model's squared error to the error of the dumbest baseline — always predicting the mean $\\bar y$:" },
        { type: "math", tex: String.raw`R^2 = 1 - \frac{\sum_i (y_i - \hat y_i)^2}{\sum_i (y_i - \bar y)^2} = 1 - \frac{\text{SS}_{\text{res}}}{\text{SS}_{\text{tot}}}` },
        { type: "p", text: "$R^2 = 1$ is perfect; $R^2 = 0$ means you did no better than predicting the mean; **$R^2 < 0$ is possible** and means your model is worse than that constant baseline (a genuinely useful red flag). Unlike RMSE, R² is unitless and comparable across datasets of different scales." },
        { type: "heading", text: "Adjusted R² — R² that doesn't reward junk features" },
        { type: "p", text: "Plain R² *never decreases* when you add a feature, even a column of random noise — so it silently encourages bloated models. **Adjusted R²** charges a penalty for each predictor $p$ over $m$ samples, so it only rises when a feature earns its keep:" },
        { type: "math", tex: String.raw`R^2_{\text{adj}} = 1 - (1 - R^2)\,\frac{m - 1}{m - p - 1}` },
        { type: "heading", text: "MAPE — percentage error for scale-free reporting" },
        { type: "math", tex: String.raw`\text{MAPE} = \frac{100\%}{m}\sum_{i=1}^m \left|\frac{y_i - \hat y_i}{y_i}\right|` },
        { type: "p", text: "MAPE expresses error as a percentage, which executives love (\"forecast is off by 8%\"). But it **explodes when any $y_i$ is near zero**, is undefined at $y_i = 0$, and asymmetrically punishes over-prediction less than under-prediction. For demand/sales forecasting it is standard; for anything that crosses zero it is dangerous — reach for sMAPE or MAE instead." },
        { type: "table",
          headers: ["Metric", "Units", "Outlier sensitivity", "Reach for it when…"],
          rows: [
            ["MSE", "target²", "very high (squared)", "training loss; big errors must dominate"],
            ["RMSE", "target", "high", "reporting error in real units"],
            ["MAE", "target", "low (robust)", "outliers/noisy labels present; want the typical error"],
            ["R²", "unitless", "high", "explaining variance; comparing across scales"],
            ["Adj. R²", "unitless", "high", "comparing models with different feature counts"],
            ["MAPE", "percent", "high near $y{=}0$", "scale-free % reporting, strictly positive targets"],
          ]
        },
        { type: "heading", text: "Residual analysis — the plot that catches what metrics hide" },
        { type: "p", text: "A single score can hide a broken model. **Plot residuals $y_i - \\hat y_i$ against predictions.** For a healthy model they scatter randomly around zero with constant spread. Warning signs: a curved/U-shaped pattern (you are missing a nonlinearity); a fan shape where spread grows with the prediction (heteroscedasticity — consider a log transform); and systematic bias above or below zero in some region (the model is miscalibrated there)." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.metrics import mean_squared_error, mean_absolute_error, r2_score\n\ny_true = np.array([3.0, -0.5, 2.0, 7.0, 4.2])\ny_pred = np.array([2.5,  0.0, 2.1, 7.8, 3.9])\n\nmse  = mean_squared_error(y_true, y_pred)\nrmse = mse ** 0.5                     # or root_mean_squared_error in new sklearn\nmae  = mean_absolute_error(y_true, y_pred)\nr2   = r2_score(y_true, y_pred)\n\ndef adjusted_r2(r2, m, p):\n    return 1 - (1 - r2) * (m - 1) / (m - p - 1)\n\nprint(f'MSE={mse:.3f} RMSE={rmse:.3f} MAE={mae:.3f} R2={r2:.3f}')\nprint('adj R2 (p=2 features):', round(adjusted_r2(r2, m=len(y_true), p=2), 3))\n\nresiduals = y_true - y_pred           # plot vs y_pred to inspect structure\nprint('mean residual (should be ~0):', residuals.mean().round(3))" },
        { type: "callout", variant: "gotcha", text: "**RMSE $\\ge$ MAE always.** By Jensen's inequality the root-mean-square of a set is never smaller than its mean absolute value; equality holds only when every error is identical. A large **gap** between RMSE and MAE is a diagnostic: it tells you a few big errors are inflating RMSE, i.e. you have outlier predictions worth investigating." },
      ]
    },

    {
      id: "cross-validation",
      title: "Cross-validation: honest estimates from limited data",
      level: "core",
      body: [
        { type: "p", text: "A single train/test split wastes data and gives a noisy, luck-of-the-draw estimate — split differently and your score jumps two points. **Cross-validation** reuses the data by rotating which part is held out, giving you both a lower-variance estimate *and* an error bar. It is the backbone of every honest model comparison." },
        { type: "heading", text: "k-fold: the workhorse" },
        { type: "p", text: "Split the data into $k$ equal folds. Train on $k-1$ of them, evaluate on the held-out one, and rotate so every fold serves as the test set exactly once. Average the $k$ scores; their standard deviation is your uncertainty." },
        { type: "math", tex: String.raw`\text{CV}_k = \frac{1}{k}\sum_{j=1}^{k} \ell\big(f^{(-j)},\, \text{fold}_j\big)` },
        { type: "p", text: "where $f^{(-j)}$ is the model trained on all folds except the $j$-th. $k=5$ or $k=10$ is the standard tradeoff: larger $k$ means more training data per fold (less pessimistic bias) but $k$ times the compute and more correlated estimates." },
        { type: "heading", text: "Stratified k-fold: preserve the class balance" },
        { type: "p", text: "Plain k-fold can, by chance, put almost all of a rare class into one fold. **Stratified** k-fold makes each fold's class proportions match the whole dataset's. For classification — especially imbalanced classification — stratified is the default, and scikit-learn's `cross_val_score` uses it automatically for classifiers." },
        { type: "callout", variant: "gotcha", text: "**Never stratify or shuffle time series.** Shuffling lets the model peek at the future to predict the past — a lookahead leak that inflates your score and evaporates in production. Time-ordered data needs the split below, not k-fold." },
        { type: "heading", text: "Leave-one-out (LOOCV): the extreme" },
        { type: "p", text: "Set $k = m$: each fold is a *single* example. LOOCV is nearly unbiased (each model sees $m-1$ points) but has two costs — you train $m$ separate models (brutal for large $m$), and the estimate has **high variance** because the $m$ training sets are almost identical, so their errors are highly correlated. Use it only for very small datasets." },
        { type: "heading", text: "Time-series split: respect the arrow of time" },
        { type: "p", text: "For temporal data, always train on the past and test on the future. A **rolling/expanding-window** split grows the training set forward in time and always tests on a later block:" },
        { type: "code", lang: "text", code: "TimeSeriesSplit (expanding window), no lookahead:\n  fold 1:  train [--------]           test [==]\n  fold 2:  train [-----------]        test [==]\n  fold 3:  train [--------------]     test [==]\n  fold 4:  train [-----------------]  test [==]\n  time --------------------------------------->" },
        { type: "heading", text: "Nested CV: the only honest way to report a tuned model" },
        { type: "p", text: "Here is the mistake almost everyone makes: they use cross-validation to *pick hyperparameters*, then report that same CV score as the model's performance. But the score is optimistic — you *selected* the config that happened to do best on those folds, so the CV score is contaminated by that selection, exactly the Goodhart problem again." },
        { type: "p", text: "**Nested CV** fixes it with two loops. The **inner loop** searches hyperparameters; the **outer loop** measures how the *whole tuning procedure* generalizes, on data the inner loop never touched." },
        { type: "code", lang: "text", code: "for each OUTER fold (measures generalization):\n    hold out outer-test\n    for each INNER fold on the remaining data:\n        try each hyperparameter config, pick the best\n    refit best config on all of outer-train\n    score it ONCE on outer-test   <- honest, never used for selection\nreport mean +/- std of the outer-test scores" },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.datasets import load_breast_cancer\nfrom sklearn.svm import SVC\nfrom sklearn.model_selection import (KFold, StratifiedKFold,\n    GridSearchCV, cross_val_score, TimeSeriesSplit)\n\nX, y = load_breast_cancer(return_X_y=True)\n\n# Plain 5-fold (stratified automatically for a classifier):\nscores = cross_val_score(SVC(), X, y, cv=5, scoring='f1')\nprint(f'5-fold F1: {scores.mean():.3f} +/- {scores.std():.3f}')\n\n# Nested CV: inner loop tunes C, outer loop measures honestly.\ninner = StratifiedKFold(5, shuffle=True, random_state=0)\nouter = StratifiedKFold(5, shuffle=True, random_state=1)\ngrid  = GridSearchCV(SVC(), {'C': [0.1, 1, 10, 100]},\n                     scoring='f1', cv=inner)\nnested = cross_val_score(grid, X, y, cv=outer, scoring='f1')\nprint(f'nested (honest) F1: {nested.mean():.3f} +/- {nested.std():.3f}')\n\n# Time-ordered data: expanding window, never shuffle.\ntscv = TimeSeriesSplit(n_splits=4)\nfor tr, te in tscv.split(X):\n    assert tr.max() < te.min()      # train strictly precedes test" },
        { type: "callout", variant: "tip", text: "**When is the extra cost of nested CV worth it?** Whenever you both tune hyperparameters *and* need to report an unbiased performance number — papers, model cards, go/no-go decisions. For quick internal iteration a single held-out validation set is fine. The rule: the number you put in front of stakeholders should never have been used to make a choice." },
      ]
    },

    {
      id: "bias-variance",
      title: "Bias, variance, and reading the diagnostic plots",
      level: "core",
      body: [
        { type: "p", text: "Every generalization error decomposes into three parts. Understanding this split turns model debugging from guesswork into diagnosis: it tells you whether to add capacity or remove it, whether to gather more data or stop wasting money collecting it." },
        { type: "math", tex: String.raw`\mathbb{E}\big[(y - \hat f(x))^2\big] = \underbrace{\big(\text{Bias}[\hat f(x)]\big)^2}_{\text{too simple}} + \underbrace{\text{Var}[\hat f(x)]}_{\text{too sensitive}} + \underbrace{\sigma^2}_{\text{irreducible}}` },
        { type: "p", text: "**Bias** is error from wrong assumptions — the model is too simple to capture the truth (a line fitting a curve). **Variance** is error from sensitivity to the particular training set — the model memorizes noise and changes wildly if you resample the data. **$\\sigma^2$** is irreducible noise in the labels themselves; no model can beat it, and pretending you can is how you overfit." },
        { type: "table",
          headers: ["Regime", "Bias", "Variance", "Symptom"],
          rows: [
            ["**Underfitting**", "high", "low", "train error *and* validation error both high, close together"],
            ["**Good fit**", "low", "low", "both errors low, small gap"],
            ["**Overfitting**", "low", "high", "train error low, validation error much higher — a big gap"],
          ]
        },
        { type: "heading", text: "Validation curves — diagnose from a complexity knob" },
        { type: "p", text: "Fix everything, sweep *one* complexity hyperparameter (tree depth, polynomial degree, regularization strength), and plot **train vs. validation score against it**. The classic U-shape appears: at low complexity both scores are poor (underfitting, high bias); as complexity rises validation improves then *turns back up* as the gap to training opens (overfitting, high variance). The sweet spot is the bottom of the validation curve — the right regularization." },
        { type: "heading", text: "Learning curves — diagnose from dataset size" },
        { type: "p", text: "Fix the model, vary the *number of training examples*, and plot train vs. validation error against training-set size. This answers the most valuable practical question: **will more data help?**" },
        { type: "table",
          headers: ["What the learning curve shows", "Diagnosis", "What to do"],
          rows: [
            ["Both errors high, converged, small gap", "high bias (underfit)", "add capacity/features; more data won't help"],
            ["Large persistent gap, val error still falling", "high variance (overfit)", "gather more data, regularize, simplify"],
            ["Both errors low, converged, small gap", "well-fit", "ship it; tune elsewhere"],
          ]
        },
        { type: "callout", variant: "tip", text: "**The single most useful debugging move.** Before touching architecture, plot the learning curve. If the curves have already converged with a small gap, more data is wasted money — you need a *better model*. If there is a big gap and the validation line is still dropping, stop tuning and go collect data. This one plot routinely saves weeks." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.tree import DecisionTreeClassifier\nfrom sklearn.model_selection import validation_curve, learning_curve\nfrom sklearn.datasets import load_breast_cancer\n\nX, y = load_breast_cancer(return_X_y=True)\n\n# Validation curve: sweep tree depth (the complexity knob).\ndepths = range(1, 20)\ntr, va = validation_curve(DecisionTreeClassifier(random_state=0),\n    X, y, param_name='max_depth', param_range=depths, cv=5, scoring='f1')\nbest = depths[np.argmax(va.mean(1))]\nprint('best max_depth (val F1 peak):', best)   # beyond here -> overfitting\n\n# Learning curve: does more data help?\nsizes, tr2, va2 = learning_curve(DecisionTreeClassifier(max_depth=best),\n    X, y, train_sizes=np.linspace(0.1, 1.0, 8), cv=5, scoring='f1')\ngap = tr2.mean(1)[-1] - va2.mean(1)[-1]\nprint(f'final train-val gap: {gap:.3f}  ->  '\n      + ('overfit, get more data' if gap > 0.05 else 'converged'))" },
      ]
    },

    {
      id: "hyperparameter-tuning",
      title: "Hyperparameter tuning: grid, random, and Bayesian search",
      level: "core",
      body: [
        { type: "p", text: "Hyperparameters are the knobs you set *before* training (tree depth, learning rate, regularization $\\lambda$) — as opposed to parameters, which are *learned*. You cannot learn them by gradient descent on the training loss, so you search: propose configurations, score each by cross-validation, keep the best." },
        { type: "heading", text: "Grid search — exhaustive and cursed by dimensionality" },
        { type: "p", text: "Define a discrete grid per hyperparameter and try **every combination**. It is simple, reproducible, and embarrassingly parallel. Its fatal flaw is combinatorial: with $d$ hyperparameters and $v$ values each you evaluate $v^d$ configs. Five hyperparameters, five values, 5-fold CV = $5^5 \\times 5 = 15{,}625$ model fits. Grid search only survives in low dimensions." },
        { type: "heading", text: "Random search — why sampling usually beats the grid" },
        { type: "p", text: "**Random search** samples $n$ configurations from distributions over the hyperparameters. Bergstra & Bengio (2012) showed it typically finds *better* models than grid search in the *same budget*. The reason is geometric and worth internalizing:" },
        { type: "callout", variant: "note", text: "**Bergstra & Bengio's insight.** In practice only a few hyperparameters actually matter, but you don't know which. A grid with 5 values per axis tests each *important* axis at only 5 distinct points — the other axes waste the budget. Random search with the same number of trials tests the important axis at $n$ *distinct* points, because no two random samples share a coordinate. You get far denser coverage of the dimensions that matter, for free." },
        { type: "math", tex: String.raw`\text{Grid: } v \text{ distinct values on each axis} \quad\text{vs.}\quad \text{Random: } n \text{ distinct values on every axis}` },
        { type: "p", text: "Practical rules: sample **learning rates and regularization on a log scale** (`loguniform`), not linearly — you care about orders of magnitude. And random search lets you spend exactly the budget you have; you are not forced to finish a grid." },
        { type: "heading", text: "Bayesian optimization — learn from each trial" },
        { type: "p", text: "Grid and random search are *memoryless*: they never learn from results so far. **Bayesian optimization** builds a probabilistic surrogate model (a Gaussian process or a tree-based estimator like TPE) of \"config → score,\" then uses an **acquisition function** to pick the next config that best balances *exploiting* promising regions against *exploring* uncertain ones. It reaches good configs in far fewer evaluations — decisive when each fit is expensive (deep nets, big data)." },
        { type: "p", text: "**Optuna** is the modern go-to: a `study` samples with TPE by default, prunes hopeless trials early, and its `suggest_*` API defines the search space in plain Python inside the objective." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom scipy.stats import loguniform\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.model_selection import GridSearchCV, RandomizedSearchCV\nfrom sklearn.datasets import load_breast_cancer\n\nX, y = load_breast_cancer(return_X_y=True)\n\n# Grid search: exhaustive over a small grid.\ngrid = GridSearchCV(RandomForestClassifier(random_state=0),\n    {'n_estimators': [100, 300], 'max_depth': [4, 8, None]},\n    cv=5, scoring='f1', n_jobs=-1)\ngrid.fit(X, y)\nprint('grid best:', grid.best_params_, round(grid.best_score_, 3))\n\n# Random search: sample 20 configs, log-scale where it matters.\nrand = RandomizedSearchCV(RandomForestClassifier(random_state=0),\n    {'n_estimators': range(100, 500),\n     'max_depth': [4, 8, 16, None],\n     'min_samples_leaf': loguniform(1e-3, 1e-1)},\n    n_iter=20, cv=5, scoring='f1', random_state=0, n_jobs=-1)\nrand.fit(X, y)\nprint('random best:', rand.best_score_.round(3))" },
        { type: "code", lang: "py", code: "# Bayesian optimization with Optuna (pip install optuna).\nimport optuna\nfrom sklearn.ensemble import RandomForestClassifier\nfrom sklearn.model_selection import cross_val_score\n\ndef objective(trial):\n    params = dict(\n        n_estimators=trial.suggest_int('n_estimators', 100, 500),\n        max_depth=trial.suggest_int('max_depth', 3, 20),\n        # log scale for anything spanning orders of magnitude:\n        min_samples_leaf=trial.suggest_float('min_samples_leaf', 1e-3, 1e-1, log=True),\n    )\n    model = RandomForestClassifier(**params, random_state=0)\n    return cross_val_score(model, X, y, cv=5, scoring='f1').mean()\n\nstudy = optuna.create_study(direction='maximize')\nstudy.optimize(objective, n_trials=40, show_progress_bar=False)\nprint('optuna best:', study.best_params, round(study.best_value, 3))" },
        { type: "callout", variant: "warn", text: "**Tuning inflates the score you tune on.** Every config you try is a peek; the best-of-N is optimistically biased. Report tuned performance on a *separate* outer test set or via nested CV (previous section), never the `best_score_` you optimized. And put preprocessing inside a `Pipeline` so the CV split refits the scaler each fold — otherwise you have leaked." },
      ]
    },

    {
      id: "class-imbalance",
      title: "Handling class imbalance",
      level: "core",
      body: [
        { type: "p", text: "Fraud, disease, churn, defects — the interesting class is usually the rare one, sometimes 1 in 1000. Trained naively, a model discovers that ignoring the minority class barely dents the loss, and collapses to the majority. There are three families of fixes, and the metric you report matters as much as the fix." },
        { type: "heading", text: "1. Resampling the data" },
        { type: "p", text: "**Random undersampling** discards majority examples until balanced — fast, but throws away data. **Random oversampling** duplicates minority examples — keeps data but risks overfitting to the copies. **SMOTE** (Synthetic Minority Over-sampling Technique) is the clever middle path: instead of copying, it *synthesizes* new minority points by interpolating between a minority example and its $k$ nearest minority neighbours:" },
        { type: "math", tex: String.raw`x_{\text{new}} = x_i + \delta\,(x_{\text{nn}} - x_i), \qquad \delta \sim \text{Uniform}(0, 1)` },
        { type: "callout", variant: "gotcha", text: "**Resample inside the CV loop, never before it.** If you SMOTE the whole dataset and *then* cross-validate, synthetic points derived from a sample can land in both train and validation folds — a leak that fakes a great score. Use `imblearn`'s `Pipeline`, which resamples *only the training fold* each split. And never resample the test set at all: it must reflect the real, imbalanced world." },
        { type: "heading", text: "2. Class weights — reweight the loss instead of the data" },
        { type: "p", text: "Cleaner than resampling for many models: tell the algorithm that a mistake on the rare class costs more. Setting `class_weight='balanced'` scales each class's loss inversely to its frequency, $w_c = m / (C \\cdot m_c)$, so the minority regains equal say without duplicating or discarding a single row. Supported by logistic regression, SVMs, random forests, and most gradient-boosting libraries (`scale_pos_weight`)." },
        { type: "heading", text: "3. Threshold moving — decide at inference time" },
        { type: "p", text: "Often the model's *probabilities* are fine and only the default 0.5 cutoff is wrong for a skewed base rate. Leave training alone and **lower the decision threshold** to trade precision for recall, choosing the operating point on the PR curve that matches your costs (the threshold-selection tools from the ROC/PR section apply directly). It is the cheapest fix and frequently the only one you need." },
        { type: "callout", variant: "warn", text: "**The metric is half the battle.** None of these help if you still report accuracy. On imbalanced data use **PR-AUC, per-class recall/precision, F1, or balanced accuracy** ($=\\tfrac12(\\text{TPR}+\\text{TNR})$). Optimizing the wrong metric wastes the best resampling in the world." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.model_selection import cross_val_score, StratifiedKFold\nfrom imblearn.over_sampling import SMOTE\nfrom imblearn.pipeline import Pipeline    # imblearn's Pipeline resamples train-only\nfrom sklearn.datasets import make_classification\n\nX, y = make_classification(n_samples=5000, weights=[0.98, 0.02],\n                           n_informative=6, random_state=0)   # 2% positive\ncv = StratifiedKFold(5, shuffle=True, random_state=0)\n\n# Option A: class weights (no data change).\nw = LogisticRegression(class_weight='balanced', max_iter=1000)\nprint('weighted  PR-AUC:', cross_val_score(w, X, y, cv=cv,\n                              scoring='average_precision').mean().round(3))\n\n# Option B: SMOTE INSIDE the pipeline (no leakage).\nsmote = Pipeline([('smote', SMOTE(random_state=0)),\n                  ('clf', LogisticRegression(max_iter=1000))])\nprint('SMOTE     PR-AUC:', cross_val_score(smote, X, y, cv=cv,\n                              scoring='average_precision').mean().round(3))" },
      ]
    },

    {
      id: "significance",
      title: "Is the difference real? Significance, bootstrap CIs, and test-set overfitting",
      level: "core",
      body: [
        { type: "p", text: "Model A scores 0.912, model B scores 0.908. Ship A? Not so fast — that gap may be noise. Reporting a point estimate without an uncertainty band is the most common way practitioners fool themselves and their stakeholders. You need *error bars* and, ideally, a *test*." },
        { type: "heading", text: "Bootstrap confidence intervals — an error bar for any metric" },
        { type: "p", text: "The **bootstrap** estimates the sampling distribution of *any* statistic with no formula and no distributional assumptions. Resample your test set *with replacement* $B$ times, recompute the metric on each resample, and read the confidence interval straight off the percentiles of those $B$ values:" },
        { type: "math", tex: String.raw`\text{CI}_{95\%} = \big[\,\hat\theta^*_{(2.5)},\; \hat\theta^*_{(97.5)}\,\big], \qquad \hat\theta^*_b = \text{metric on bootstrap sample } b` },
        { type: "p", text: "If A's and B's confidence intervals overlap heavily, that 0.4-point gap is well within noise — do not claim A is better. A CI turns \"A beats B\" into the honest \"A scores 0.912 (95% CI 0.898–0.925).\"" },
        { type: "heading", text: "Paired tests — comparing two models on the same data" },
        { type: "p", text: "Because both models are scored on the *same* examples, compare them **paired**, not independently — this cancels the variance from which examples happened to be in the test set and is far more sensitive. For per-example errors, a paired test on the differences; across CV folds, the corrected resampled t-test (Nadeau–Bengio) or McNemar's test on the two models' disagreement counts for classification. Treat $p < 0.05$ as a weak signal, not gospel." },
        { type: "callout", variant: "warn", text: "**The leaderboard trap (test-set overfitting).** Evaluate 500 model variants against the same test set and pick the best, and you have *fit the test set* — the winner's lead is partly luck that won't reproduce. This is exactly why Kaggle has a private leaderboard, and why teams that top the public one routinely tumble on the private split. The test set has a limited number of honest looks in it; spend them wisely." },
        { type: "callout", variant: "tip", text: "**The multiple-comparisons corollary.** Try enough hypotheses and one clears $p<0.05$ by chance alone (1 in 20). If you compare many models, correct for it (Bonferroni, or just be skeptical) — or better, keep an untouched final holdout that no model-selection decision ever saw, and report on that once." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.metrics import roc_auc_score\n\nrng = np.random.default_rng(0)\n# y_true, scores_A, scores_B from a held-out test set:\ny = rng.integers(0, 2, 400)\nsA = np.clip(y * 0.3 + rng.normal(0.5, 0.3, 400), 0, 1)\nsB = np.clip(y * 0.28 + rng.normal(0.5, 0.3, 400), 0, 1)\n\ndef bootstrap_auc_ci(y, s, B=2000, seed=0):\n    rng = np.random.default_rng(seed)\n    n = len(y)\n    stats = []\n    for _ in range(B):\n        idx = rng.integers(0, n, n)              # resample WITH replacement\n        if len(np.unique(y[idx])) < 2:           # need both classes present\n            continue\n        stats.append(roc_auc_score(y[idx], s[idx]))\n    lo, hi = np.percentile(stats, [2.5, 97.5])\n    return np.mean(stats), lo, hi\n\nmA, loA, hiA = bootstrap_auc_ci(y, sA)\nmB, loB, hiB = bootstrap_auc_ci(y, sB)\nprint(f'A: {mA:.3f}  95% CI [{loA:.3f}, {hiA:.3f}]')\nprint(f'B: {mB:.3f}  95% CI [{loB:.3f}, {hiB:.3f}]')\n# Paired bootstrap of the DIFFERENCE is even better: resample once, score both.\ndiffs = []\nrng = np.random.default_rng(1)\nfor _ in range(2000):\n    idx = rng.integers(0, len(y), len(y))\n    if len(np.unique(y[idx])) < 2: continue\n    diffs.append(roc_auc_score(y[idx], sA[idx]) - roc_auc_score(y[idx], sB[idx]))\nlo, hi = np.percentile(diffs, [2.5, 97.5])\nprint(f'A-B diff 95% CI [{lo:.3f}, {hi:.3f}] '\n      + ('-> real' if lo > 0 else '-> not significant (CI spans 0)'))" },
      ]
    },

    {
      id: "data-leakage",
      title: "Data leakage: the career-saving section",
      level: "core",
      body: [
        { type: "p", text: "If you learn one thing from this entire chapter, learn this. **Data leakage** is when information that would not be available at prediction time sneaks into training. The signature is unmistakable and seductive: spectacular validation scores that *collapse* in production. It is the single most common reason ML projects fail *after* looking like a success — and spotting it is what separates practitioners from tutorial-followers." },
        { type: "heading", text: "Type 1: Target leakage — a feature that encodes the answer" },
        { type: "p", text: "A feature is available in your table but would not exist (or would have a different value) at the moment you actually need to predict. Classic cases: a `has_paid` column when predicting default; a `days_in_hospital` feature when predicting admission; an `account_closed_date` when predicting churn. The feature is a proxy for the label, so the model \"learns\" to read the answer off the exam. 0.99 AUC, useless model." },
        { type: "callout", variant: "gotcha", text: "**The leakage tell.** A single feature with implausibly high importance, or a model that is suspiciously *perfect*, should trigger immediate suspicion — not celebration. Ask of every feature: *would I actually know this value at the instant I have to make the prediction?* If not, it leaks. This one question catches most target leakage." },
        { type: "heading", text: "Type 2: Train/test contamination — fitting on the test set" },
        { type: "p", text: "You compute something using the whole dataset *before* splitting, so statistics from the test set bleed into training. The subtle, ubiquitous offender is **preprocessing before splitting**: fit a `StandardScaler` (or PCA, or an imputer, or feature selection, or target encoding) on all the data, then split. Now your scaler's mean and std were computed partly from test rows — the test set is no longer unseen." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.linear_model import LogisticRegression\nfrom sklearn.pipeline import make_pipeline\nfrom sklearn.model_selection import cross_val_score, train_test_split\nfrom sklearn.datasets import load_breast_cancer\n\nX, y = load_breast_cancer(return_X_y=True)\n\n# WRONG: scaler sees the whole dataset (incl. future test folds) -> leak.\nX_leaked = StandardScaler().fit_transform(X)         # fit on everything!\nbad = cross_val_score(LogisticRegression(max_iter=5000),\n                      X_leaked, y, cv=5).mean()\n\n# RIGHT: put preprocessing in a Pipeline so it refits INSIDE each fold.\npipe = make_pipeline(StandardScaler(), LogisticRegression(max_iter=5000))\ngood = cross_val_score(pipe, X, y, cv=5).mean()      # scaler fit on train fold only\n\nprint(f'leaked  CV: {bad:.4f}')\nprint(f'honest  CV: {good:.4f}')   # the honest number is the one you trust" },
        { type: "heading", text: "The leaky preprocessing hall of shame" },
        { type: "table",
          headers: ["Operation", "Why it leaks if done before the split", "Safe practice"],
          rows: [
            ["Scaling / normalization", "test mean & std enter the transform", "fit scaler on train fold only (Pipeline)"],
            ["Imputation of missing values", "fill value computed from test rows", "fit imputer inside CV"],
            ["Feature selection", "selection 'sees' test-set correlations", "select within each fold"],
            ["Target / mean encoding", "encodes using test-set targets", "use out-of-fold or smoothed encoding"],
            ["Oversampling / SMOTE", "synthetic points span train & test", "resample train fold only (imblearn Pipeline)"],
            ["Deduplication done wrong", "near-duplicate rows split across sets", "group-split so dupes stay together"],
          ]
        },
        { type: "heading", text: "Type 3: Temporal & group leakage" },
        { type: "p", text: "**Temporal:** shuffling time-ordered data so future rows train a model tested on the past — always use `TimeSeriesSplit`. **Group:** the same entity (patient, user, customer) appears in both train and test; the model memorizes the entity rather than the pattern. If rows are grouped, split by *group* (`GroupKFold`) so no group straddles the boundary — otherwise your generalization estimate is a memorization estimate." },
        { type: "callout", variant: "good", text: "**Leakage-proof workflow.** (1) Split first, before touching the data. (2) Wrap *every* transform in a `Pipeline` so it fits on train only. (3) Interrogate suspiciously good scores instead of celebrating them. (4) For each feature ask \"is this known at prediction time?\". (5) Split by time for temporal data and by group for grouped data. Follow these and you will avoid the mistake that has quietly killed more ML projects than any modeling error." },
      ]
    },

    {
      id: "sklearn-example",
      title: "Worked example: end-to-end evaluation in scikit-learn",
      level: "core",
      body: [
        { type: "p", text: "Everything so far, assembled into one honest pipeline: leakage-safe preprocessing, cross-validated tuning, a held-out test set touched exactly once, and a full report with ROC data. This is the template to keep." },
        { type: "code", lang: "py", code: "import numpy as np\nfrom sklearn.datasets import load_breast_cancer\nfrom sklearn.model_selection import train_test_split, GridSearchCV, cross_val_score\nfrom sklearn.pipeline import make_pipeline\nfrom sklearn.preprocessing import StandardScaler\nfrom sklearn.svm import SVC\nfrom sklearn.metrics import (classification_report, confusion_matrix,\n                             roc_curve, roc_auc_score, average_precision_score)\n\nX, y = load_breast_cancer(return_X_y=True)\n\n# 1) Split FIRST. The test set is now sacred until the very end.\nX_tr, X_te, y_tr, y_te = train_test_split(\n    X, y, test_size=0.2, stratify=y, random_state=42)\n\n# 2) Preprocessing lives in the Pipeline -> refit inside every CV fold (no leak).\npipe = make_pipeline(StandardScaler(), SVC(probability=True, random_state=0))\n\n# 3) Cross-validated hyperparameter search on the TRAINING data only.\nparam_grid = {'svc__C': [0.1, 1, 10, 100],\n              'svc__gamma': ['scale', 0.01, 0.001]}\nsearch = GridSearchCV(pipe, param_grid, cv=5, scoring='f1', n_jobs=-1)\nsearch.fit(X_tr, y_tr)\nprint('best params :', search.best_params_)\nprint('best CV F1  :', round(search.best_score_, 3))\n\n# 4) Quick CV sanity band on the chosen config (mean +/- std).\ncv = cross_val_score(search.best_estimator_, X_tr, y_tr, cv=5, scoring='f1')\nprint(f'CV F1 = {cv.mean():.3f} +/- {cv.std():.3f}')\n\n# 5) Touch the test set ONCE. This is the number you report.\ny_pred  = search.predict(X_te)\ny_score = search.predict_proba(X_te)[:, 1]\n\nprint('\\nConfusion matrix:\\n', confusion_matrix(y_te, y_pred))\nprint('\\n', classification_report(y_te, y_pred, digits=3))\nprint('ROC-AUC :', round(roc_auc_score(y_te, y_score), 3))\nprint('PR-AUC  :', round(average_precision_score(y_te, y_score), 3))\n\n# 6) ROC curve points for plotting / threshold selection.\nfpr, tpr, thr = roc_curve(y_te, y_score)\nj = tpr - fpr\nprint('Youden-optimal threshold:', round(float(thr[np.argmax(j)]), 3))" },
        { type: "code", lang: "text", code: "Typical output (numbers vary by split):\n  best params : {'svc__C': 10, 'svc__gamma': 'scale'}\n  best CV F1  : 0.978\n  CV F1 = 0.978 +/- 0.011\n\n  Confusion matrix:\n   [[41  2]\n    [ 1 70]]\n\n                precision    recall  f1-score   support\n             0      0.976     0.953     0.965        43\n             1      0.972     0.986     0.979        71\n      accuracy                          0.974       114\n     macro avg      0.974     0.970     0.972       114\n\n  ROC-AUC : 0.995\n  PR-AUC  : 0.997\n  Youden-optimal threshold: 0.512" },
        { type: "callout", variant: "tip", text: "**Read the report top to bottom.** Per-class precision/recall exposes whether one class is being neglected; `support` shows the class sizes (watch for imbalance); `macro avg` treats classes equally while `weighted avg` follows frequency. The CV band ($\\pm 0.011$) tells you whether a rival model's difference is even meaningful. The test-set line is the *only* number you present as performance." },
      ]
    },

    {
      id: "projects",
      title: "Projects & practice",
      level: "core",
      body: [
        { type: "callout", variant: "note", text: "Evaluation is a *skill*, and skills need reps. Each project forces a specific judgment call you cannot learn by reading. Do at least three; the leakage one is non-negotiable." },
        { type: "list", ordered: true, items: [
          "**Build the confusion matrix and every metric from scratch.** Implement `confusion_matrix`, precision, recall, F1, specificity, and ROC-AUC (via the rank/probability definition) in pure NumPy. Verify each against scikit-learn on a real dataset. You will never be confused by a metric again once you have coded it.",
          "**Metric-mismatch demo on imbalance.** Make a 1%-positive dataset. Train a naive model, then show accuracy (~0.99) next to recall (~0) and PR-AUC. Now fix it three ways — class weights, SMOTE (inside the pipeline!), threshold moving — and tabulate how each moves precision, recall, and PR-AUC. Write one sentence on which you'd deploy and why.",
          "**Diagnose from plots.** For a decision tree and a polynomial regression, generate validation curves (sweep depth / degree) and learning curves (sweep dataset size). From the plots alone, label each regime underfit/good/overfit and decide whether more data would help. Confirm your call by acting on it.",
          "**Deliberately plant — then catch — a leak.** Take any dataset and (a) scale before splitting, then (b) inside a pipeline; report the gap. Then engineer a target-leak feature (a near-copy of the label) and watch AUC hit ~1.0. This is the muscle memory that saves your career.",
          "**Random vs. grid vs. Bayesian, same budget.** Give grid search, random search, and Optuna an identical number of trials on one model. Plot best-score-so-far vs. trial count. Reproduce Bergstra & Bengio's result that random beats grid, and see Optuna pull ahead when fits are expensive.",
          "**Honest model comparison with the bootstrap.** Train two models, compute paired bootstrap confidence intervals for the difference in your chosen metric, and only claim a winner if the CI excludes zero. Then re-run selection over 100 variants against one test set and watch the 'winner' fail to reproduce on a fresh holdout.",
        ]},
      ]
    },

    {
      id: "references",
      title: "Go deeper (references)",
      level: "deep",
      body: [
        { type: "p", text: "Evaluation rewards depth more than almost any ML topic — the failure modes are subtle and the literature is excellent. In recommended order:" },
        { type: "link", url: "https://scikit-learn.org/stable/modules/model_evaluation.html", text: "scikit-learn User Guide — Model evaluation (the canonical, exhaustive reference for every metric and its API)" },
        { type: "link", url: "https://scikit-learn.org/stable/modules/cross_validation.html", text: "scikit-learn User Guide — Cross-validation (KFold, stratified, TimeSeriesSplit, GroupKFold, nested CV, with diagrams)" },
        { type: "link", url: "https://www.jmlr.org/papers/volume13/bergstra12a/bergstra12a.pdf", text: "Bergstra & Bengio (2012) — Random Search for Hyper-Parameter Optimization (the paper behind 'random usually beats grid')" },
        { type: "link", url: "https://optuna.readthedocs.io/en/stable/", text: "Optuna documentation — modern Bayesian/TPE hyperparameter optimization with pruning" },
        { type: "link", url: "https://www.jair.org/index.php/jair/article/view/10302", text: "Chawla et al. (2002) — SMOTE: Synthetic Minority Over-sampling Technique (the original)" },
        { type: "link", url: "https://developers.google.com/machine-learning/crash-course/classification", text: "Google ML Crash Course — Classification (accuracy, precision/recall, ROC/AUC, thresholds — excellent visual intuition)" },
        { type: "link", url: "https://hastie.su.domains/ElemStatLearn/", text: "Hastie, Tibshirani & Friedman — The Elements of Statistical Learning, Ch. 7 (Model Assessment & Selection; free PDF)" },
      ]
    },
  ],

  packages: [
    { name: "scikit-learn", why: "`metrics`, `model_selection` — the reference implementation of everything in this chapter" },
    { name: "sklearn.model_selection", why: "`cross_val_score`, `GridSearchCV`, `StratifiedKFold`, `TimeSeriesSplit`, `learning_curve`" },
    { name: "sklearn.metrics", why: "`classification_report`, `roc_curve`, `confusion_matrix`, `average_precision_score`" },
    { name: "sklearn.pipeline", why: "`Pipeline` / `make_pipeline` — the leakage-proof way to bundle preprocessing with the model" },
    { name: "imbalanced-learn", why: "`SMOTE` and an imblearn `Pipeline` that resamples the training fold only (no leakage)" },
    { name: "optuna", why: "Bayesian/TPE hyperparameter search with early pruning — few trials, expensive fits" },
    { name: "numpy", why: "implement metrics and bootstrap resampling from scratch to truly understand them" },
    { name: "matplotlib", why: "ROC/PR curves, validation & learning curves, residual plots — evaluation is visual" },
  ],

  gotchas: [
    "Accuracy on imbalanced data is a trap: 99.5% negatives means 'always predict negative' scores 99.5% and catches nothing. Report precision/recall/F1/PR-AUC.",
    "Never fit preprocessing (scaler, imputer, PCA, encoder) before the split — put it in a `Pipeline` so it refits on the training fold only. This is the #1 leakage source.",
    "ROC-AUC is over-optimistic on severe imbalance because $TN$ dominates FPR's denominator — switch to PR-AUC when positives are rare.",
    "Tuning inflates the score you tune on: `best_score_` from GridSearchCV is optimistic. Report on an untouched test set or via nested CV.",
    "Don't shuffle or stratify time series — it creates lookahead leakage. Use `TimeSeriesSplit`; for grouped rows use `GroupKFold`.",
    "A single feature with implausibly high importance, or a suspiciously perfect model, means target leakage — interrogate it, don't celebrate it.",
    "$R^2 < 0$ is legal and means your model is worse than predicting the mean $\\bar y$ — a useful red flag, not a bug.",
    "Resample (SMOTE) inside the CV loop and never on the test set; synthetic points leaking across folds fake a great score.",
  ],

  flashcards: [
    { q: "Define precision and recall, and when each is the priority.", a: "Precision $= TP/(TP+FP)$ — of the alarms raised, how many were real; prioritize when false alarms are costly. Recall $= TP/(TP+FN)$ — of real positives, how many caught; prioritize when misses are costly." },
    { q: "Why is F1 the harmonic mean, not the arithmetic mean, of precision and recall?", a: "The harmonic mean is dominated by the smaller value, so it refuses to let a great precision hide a terrible recall (or vice versa). If either is 0, $F_1 = 0$." },
    { q: "What does ROC-AUC mean probabilistically?", a: "$P(\\text{score of a random positive} > \\text{score of a random negative})$ — the probability the model ranks a positive above a negative. Threshold-independent ranking quality." },
    { q: "When should you prefer PR-AUC over ROC-AUC?", a: "On severe class imbalance. ROC-AUC's FPR has the huge $TN$ count in its denominator, so it looks optimistic; PR-AUC focuses on the rare positive class." },
    { q: "What is nested cross-validation and why use it?", a: "Inner loop tunes hyperparameters, outer loop measures how the whole tuning procedure generalizes on data never used for selection. It gives an unbiased score for a *tuned* model." },
    { q: "How do you read under/overfitting off a learning curve?", a: "Both errors high and converged with a small gap = high bias (underfit), more data won't help. Large persistent gap with validation still falling = high variance (overfit), get more data." },
    { q: "Why does random search often beat grid search (Bergstra & Bengio)?", a: "Only a few hyperparameters matter but you don't know which. Random search gives $n$ distinct values on *every* axis; a grid gives only $v$ per axis, wasting budget on the axes that don't matter." },
    { q: "What is target leakage, and the one question that catches it?", a: "A feature encodes information unavailable at prediction time (e.g. `has_paid` when predicting default). Ask: 'would I actually know this value at the instant I predict?' If not, it leaks." },
    { q: "Why must preprocessing go inside a Pipeline for cross-validation?", a: "So the scaler/imputer/encoder is fit on the training fold only. Fitting on all data before splitting lets test-set statistics leak in, faking a better score." },
    { q: "How does a bootstrap confidence interval work?", a: "Resample the test set with replacement $B$ times, recompute the metric each time, and take the 2.5th/97.5th percentiles of those values as the 95% CI. No distributional assumptions." },
    { q: "Three ways to handle class imbalance?", a: "(1) Resample — SMOTE synthesizes minority points by interpolation, done inside the CV loop; (2) class weights — `class_weight='balanced'` reweights the loss; (3) threshold moving — lower the 0.5 cutoff at inference." },
    { q: "Why report RMSE vs MAE, and what does a big gap between them mean?", a: "RMSE is in target units but outlier-sensitive (squares errors); MAE is robust and reads as the typical error. RMSE $\\ge$ MAE always; a large gap signals a few big errors inflating RMSE." },
  ],

  cheatsheet: [
    { label: "Confusion matrix", code: "confusion_matrix(y_true, y_pred)" },
    { label: "Full P/R/F1 report", code: "classification_report(y_true, y_pred, digits=3)" },
    { label: "ROC curve + AUC", code: "roc_curve(y, s); roc_auc_score(y, s)" },
    { label: "PR-AUC (imbalance)", code: "average_precision_score(y, s)" },
    { label: "Regression metrics", code: "mean_squared_error / mean_absolute_error / r2_score" },
    { label: "k-fold CV score", code: "cross_val_score(model, X, y, cv=5, scoring='f1')" },
    { label: "Stratified folds", code: "StratifiedKFold(5, shuffle=True, random_state=0)" },
    { label: "Time-series split", code: "TimeSeriesSplit(n_splits=5)" },
    { label: "Group split", code: "GroupKFold(n_splits=5)" },
    { label: "Grid search", code: "GridSearchCV(pipe, param_grid, cv=5, scoring='f1')" },
    { label: "Random search", code: "RandomizedSearchCV(est, dists, n_iter=30, cv=5)" },
    { label: "Leakage-safe pipeline", code: "make_pipeline(StandardScaler(), clf)" },
    { label: "SMOTE (no leak)", code: "imblearn.pipeline.Pipeline([('smote',SMOTE()),('clf',clf)])" },
    { label: "Learning curve", code: "learning_curve(model, X, y, train_sizes=...)" },
  ],
});
